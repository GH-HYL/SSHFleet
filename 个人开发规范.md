# 个人开发规范

**通用配置，适用于所有个人项目；路径非强制，按工程需要选用。**

**本文件为硬性约束，非参考建议：按字面执行，不延伸理解、不自行发挥——未经要求的重构、依赖、设计模式、新增文件，一律不引入。**

---

## 零、红线（违反即终止）

1. **未得明确授权，不得修改任何现有代码**——授权前只能梳理条目、整理文档。
2. **未得明确授权，不得修改本文件。**
3. **不得绕过 **`.gitignore`——禁 `git add -f` / `git add <指定文件>`；新增文件追踪须先经用户确认。

---

## 一、工作区结构

- `modules/` 各语言工程，目录名 `项目名_语言`；旧工程（**只读不改**）为 `旧工程名_bak`
- `docs/` 规则文件、经验总结等需持久化的文档
- `tools/` 辅助脚本、打包工具 | `build/` 构建中间产物 | `release/` 最终可运行产物

---

## 二、工程结构（仅 `modules/` 下的 Go、Python 工程）

- 入口文件放工程根目录，**承载主干全流程**（初始化 → 运行 → 退出）；其余文件均为其中某个环节的分支实现，由入口统一调用并回收结果
- **MUST NOT** 在子模块反向驱动主流程（回调控制生命周期、全局状态跨模块流转）
- 根目录**必须**建 `src`（Py）/ `internal`（Go）：其内只建功能目录、不放散文件，且**必须**含 log 模块
- 代码按功能分目录存放；单文件过大时按**功能边界**拆分，不按行数机械切割

---

## 三、Git 提交

- 提交信息**必须中文**，格式 `类型(可选作用域): 简短描述`；类型 `feat` | `fix` | `refactor` | `docs` | `chore` | `style` | `test`
- 功能完成且文档整理完毕后，自动提交

---

## 四、CHANGELOG

**只记用户能直接体验到的**功能新增、体验优化、问题修复。 **不记**：内部代码符号（函数 / 变量 / 模块 / 文件名）、实现细节与重构拆分、测试与构建工具链。

### CRITICAL: 读取方式

**MUST** `Read(file_path="CHANGELOG.md", limit=60)` 部分读取后再编辑（部分读取同样满足 Edit 的前置要求）。只改顶部「待定」段落，**MUST NOT** 编辑任何 `## [x.y.z]` 历史段落。

### 定版规则

- 新修改记入 `## [待定] - 起始日期`
- **转天才定版**：当天不定版；至少次日后才可定为正式版本号，日期更新为定版当天
- 新一天记录前先查遗留「待定」：有 → 先整理定版，再开新条目；无 → 直接开新条目
- 版本号按语义化版本：主 = 不兼容改动 / 次 = 向下兼容新增 / 修订 = 向下兼容修正

---