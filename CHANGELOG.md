# Changelog

> 本文件自 5.0.0 起记录；更早的版本记录属于上一套实现，不在本文件内。
>
> **版本号怎么定**：一段 = 一天的改动，段头就是版本号，没有「待定」这个中间状态——开段时先按已知内容把号定下来，当天若出现更高档位的改动，把号往上抬（只升不降）。档位取段内最高的一项：不兼容改动 → 主版本，有新增 → 次版本，只有修正 → 修订。日期写改动发生的当天，第二天开工即视为上一段封口，已封口的段落不再改动。
>
> **这是哪一次代码**：看程序帮助信息里的构建标识，形如 `版本: v5.1.0+短提交号 (提交时间)`。同一天可能编译多次，版本号相同、构建标识不同，靠它区分。

## [5.3.0] - 2026-09-17

### Added

- **执行日志交代命令是怎么交给 SSH 的**：此前只记「开始执行任务：节点 N 个……」，命令被包成什么样、内容走哪条通道、脚本用什么解释器与身份，事后全查不到。现在命令/脚本模式会多出一段「交给 SSH 执行：」，分三行——**命令行**（实际发下去的那条，如 `bash -lc 'export LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8; bash'`）、**stdin**（经标准输入送过去的内容：命令原文，或「脚本 <路径> 的内容（<解释器> 解释）」）、**说明**（先导入 `LC_ALL`/`LANG` 两个 UTF-8 环境变量，再把 stdin 的内容交给登录用户的 bash 或 root 身份的 bash 执行）。`--nobash` 时不出 stdin 行，说明里写明不套登录 shell、不经 stdin。上传/下载模式不产生这段
- **工具日志的解析结果改回旧版那种紧凑写法**：参数解析完成后，日志把结果按 `Namespace(c='who -b', s='', u='', d='', f='nodes.csv', p='', m='direct', t=60, T=10, n=None, r='v3', nobash=False, disinteractive=False, k='')` 的形状单行打印——字段名与空值写法（文本为 `''`、未指定的数值为 `None`）沿用旧版 Python 实现的习惯，熟悉旧日志的人不用重新认一套。文本值带引号、数值不带引号，一眼分得清哪是内容哪是数量

### Fixed

- **报告与日志里的执行命令补回引号**：`【执行命令】` 与「原始命令行参数」此前是空格平铺，`-c 'who -b'` 会被打成 `-c who -b`——那是另一条命令（`-b` 成了多余参数），照抄重跑跑不起来。现在逐段判断「会不会被 shell 二次拆词」，该加引号的加回去（`-c 'who -b'`）、不该加的保持原样（`-c pwd`、`-f 172.28.118.49,22,root`）
- **同一个大小不再有两种写法**：进度条与交互确认显示 `1.5 MB`，执行日志却显示 `1.50 MB`——同一块屏幕上会同时看到两种写法。现在统一为 `1.5 MB`（1024 进制、一位小数）
- `latest_history` 链接冲突时的警告改走**标准错误**，文案与工具其他警告统一成 `[警告]`：此前它单独走标准输出，输出重定向到文件时会把这条提示混进正常输出里

## [5.2.0] - 2026-09-16

### Added

- 新增失败分类「**账号过期**」「**登录被禁止**」（登录 shell 被设成 nologin）、「**仅允许 SFTP**」（目标机用 `ForceCommand internal-sftp` 限制）。这三种此前分别被标成「SSH协议错误」与「执行失败(退出码1)」，方向都是错的
- 新增失败分类「**握手被断开**」：认证阶段被目标机直接掐断、且对方没说原因时（客户端只拿到一个 EOF），分类列不再塞报错原文
- 统计末尾新增「**提示：**」块：本次出现过的分类里，解释有内容的各给一行「分类名：可能是什么」。内容在 `config/error_keywords.toml` 里按分类用 `tip` 维护——**每个分类都带这个字段**，写空串（`tip = ''`）即不显示，所以还没想好的分类先留空即可；「执行失败(退出码N)」的含义由工具按退出码自动补上——原先单列一行的「常见退出码」并进这里，不再单独出现
- 配置新增 `enable.show_category_tips`：是否显示「提示：」块；关闭时统计末尾不出这个块，只保留「常见退出码」那一行
- 报错列带上**服务端提示**：目标机在认证阶段回的话（账号过期提示、`/etc/nologin` 里写的维护通知原文等）现在会写进该节点的报错原文——以前这些内容被直接丢掉，只能上机器看日志

### Changed

- **执行日志补回异常现场**：此前每个节点只记「连接成功/失败 → 执行结果 → 分类」三行，出错时的**报错原文**（连不上的原因、会话创建失败、命令执行超时、远程路径不存在、逐文件失败）与**输出明细**（服务端拒绝语、命令输出、传输逐文件结果）全都不落日志——事后翻日志只剩一句「命令执行失败，退出码 1」，看不出为什么。现在失败节点会把这两样逐行写进日志（每行都带节点前缀与时间戳，单节点最多 50 行、超长行截断，其余指向 `output.txt`）
- **执行日志的正常记录压到最少**：成功节点由三行合为一行（`【IP】成功：连接 0.109s，执行 0.276s`），收尾的「连接统计」与「执行结束」并成一行——1000+ 节点时不再被逐节点细节刷满；**失败节点照旧逐条展开、不合并**（时间戳逐行保留，便于定位错误触发时机）
- 执行日志里失败节点的连接行补上**登录用户**与**登录方式**，私钥解析失败而退回密码时写明「密码（密钥解析失败，已回退）」——此前这条只在老引擎的日志里有，新实现是静默回退
- **工具日志开始记录致命错误**：此前 `fatal` 只往终端打，日志里一片空白——配置、清单、凭据、参数不合规、危险命令检查任何一步失败，事后都查不到原因。现在致命错误逐行写进工具日志（批量执行阶段的同时写进该次归档的执行日志）并在退出前落盘；`output.xlsx` / `results.xlsx` 生成失败、最新历史链接失败、进度界面异常退出也都留痕

### Fixed

- **密码过期不再被归类成「执行失败(退出码1)」**：命令被目标机拒绝执行时，退出码来自拒绝方而不是命令本身；现在按拒绝原因给分类（密码过期 / 登录被禁止 / 仅允许 SFTP），退出码照旧保留在明细里
- 修正「SSH协议错误」的误标：账号过期与维护窗口（`/etc/nologin` 存在）此前都落在这个分类下，会把排查引向网络与协议版本
- 判据精简：**91 条 → 55 条**。Go 侧依赖库的报错文案是固定的，此前沿用的那批「经验词」（多来自更早的 Python 实现）在现在的实现里不会出现，属永远不会命中的死词；判据自此只保留三类来源——工具自己打的文案、依赖库/标准库的固定文案、操作系统与服务端输出的报文
- 删掉几条过短判据（`has expired`、`refused` 之类），它们会把无关报错也归错类
- 补上此前漏收的实现侧文案（本地文件不可读、远程无可下载文件等），这些场景此前会退化成「原文即分类」

## [5.1.0] - 2026-09-15

### Added

- 帮助信息重排：选项表按**四列严格对齐**——短选项 / 长选项 / 方括号小括号标记 / 详细说明；短选项与长选项贴成一个整体（`-c --command`），按用途分成 4 组、组间空行分隔；每条说明重写为更短更直白的说法（`-k` 随 `-r` 归入执行参数组）；默认值标记收短（`[默认: 命令60s/上传300s]` → `[默认: 60/300]`，单位已在说明的「（秒）」里，标记只留数值、与 `-T` 的 `[默认: 10]` 一致），说明列因此从第 51 列提前到**第 43 列**（100 列终端下说明列从 48 列涨到 56 列）；长说明仍只在说明列内折行、续行同列起始，列宽按终端宽度自适应、管道下不折行。用法只留一行，示例补「生成密钥 / 转换凭据」两条
- 帮助里的版本号带**构建标识**：形如 `v5.1.0+短提交号 (提交时间)`——短提交号 + 该提交时间，工作区有未提交改动时带 `-dirty`。同一天可能编译多次、版本号相同，靠这一行才能区分「这是哪一次代码」（仓库外编译时自动退化为只显示版本号）
- 新增 `--key-status`：一条命令查看主密钥状态——本次运行读到哪把（指纹）、本机保存的是哪把、两处是否一致、下一步怎么办
- **主密钥来源一致性检测**（把「生成密钥后忘了 source / 重开终端」交给工具发现，而不是靠人记住）：
  - 开工前**预检查**：不一致 / 本机已保存但当前终端没读到 / 密钥没持久化，三种情况都会当场说清现状、影响与下一步
  - 读密钥时**警告一次**，点名两处指纹与出处——这是凭据解密报「主密钥不匹配」的常见根因
  - **加密直接拦下**（`--convert-password` 转等级 3）：用旧密钥加密出的文件，重开终端后就打不开了，属不可逆风险，不给侥幸空间；解密方向只警告不拦（文件可能正是用当前这把加密的）
- `--gen-key` 输出**密钥指纹**，覆盖前把「本次运行用哪把、本机存的是哪把」并排摆出来，不一致时点明原因

### Changed

- **进度条重做**：从「字符按百分比填充、没有动画」换成带动画的实现——细线渐变条（颜色随进度推进而流动）、弹簧缓动（进度变化平滑追上去，不是一格一格跳）、百分比带一位小数。**底部固定 + 上方滚动**的版面不变（结果明细照旧往上滚，进度区原地重绘），命令模式单行、传输模式多行（总进度 + 节点进度 + 逐节点明细）也保持不变；进度条**执行一开始就显示 0%**，不再等第一个节点回来才出现
- **进度条的耗时与统计块对齐**：计时起点改为执行开始、秒数改四舍五入——此前它的起点晚（首个节点回来才开始计），且秒数截断，同一屏上会出现进度条 `0:00:11` 与统计块 `12.03 秒` 差一秒的观感（用户 2026-09-15 追问后定位）
- **输出重定向（`> 文件` / 管道）时不再输出进度条**：那种场景下进度区整体不打、只直出结果明细，避免文件里混进成千上万条光标控制序列
- 单条结果明细的字段顺序：**分类**提到「执行」下面，命令/文件明细的 **output 原文挪到最后**（终端与 output.txt 同步）
- `output.xlsx` 按旧版规则重做：事件类型列写完整状态串（`连接: 成功 - X.XXXs` / `执行: 成功 - X.XXXs` / `分类: 名称`），命令输出**逐行单独成行**并标注「标准输出和错误输出」（顶端对齐、**单行不折行**），每条结果之间加一行**浅蓝分隔行**，冻结首行，列宽 15/20/120（C 列加宽一倍，整行输出横向铺开而非折成多行）
- `output.xlsx` 里**连接失败会多一行「错误」详情**（旧版没有这行，错误原文只能在 results.xlsx 的 `error` 列看到）
- `results.xlsx`：工作表名改为 `Results`，表头与数据单元格加**细边框**、表头蓝底白字居中；**A–M 列按内容自适应列宽**，error 与 output 两列是长短不一的自由文本、保持固定宽度不撑列——**error 列收窄到 30**（失败原因多是短句，窄一点表格更紧凑），output 列 50
- `--gen-key` 的提示补全：报告保存位置与密钥指纹、说明**当前终端仍是旧密钥**、并明确此刻执行 `--convert-password` 会被拦下
- README 全面对齐当前版本：参数表说明各参数都有**等价长选项**、工具选项补上 `--key-status`、安装包清单补上 CHANGELOG、配置示例补全 `password_security` 与 `secret_dir`；章节编号统一为 1.1–6.3，正文只描述当前版本功能，不再出现历史版本的说明

### Fixed

- **危险命令警告框的标题框线对齐**：标题的装饰符号此前写成带变体选择符的 emoji 写法（⚠️），宽度计算把它当成 2 列、终端实际只画 1 列，标题于是多算两列、居中偏移、右边框歪斜（上一轮只修了标题留白对称，没动这个前提）。现在改用不带变体选择符的写法，并修正宽度函数——变体选择符、零宽连接符这类**零宽字符不再计列**（此前把「基础码位 1 + 变体选择符 1 = 2」当成了正确依据）
- **进度条不再「先满、回落、再满」**：弹簧阻尼此前设成了欠阻尼（低于临界值的弹性），进度值跳变时会冲过目标值——冲过 100% 被截断显示成满格、回落时又经过 95%，看上去像进度在倒退。改回临界阻尼后数字只朝一个方向走，代价是少一点「弹」
- **进度条收尾时会定格到最终值**：此前它读的是弹簧动画的当前值，而收尾紧跟着最后一个节点完成到来，动画往往还停在半路——节点进度已经 72/72，条却停在 97.2%。现在退出前先渲染一帧「终帧」（各条按目标值直接定格）再退出，四种进度条（命令模式执行进度、传输模式的总进度 / 节点进度 / 逐节点明细）全部闭环
- **命令模式执行结束后进度条不再消失**：此前它会被连同最后一行一起擦掉——进度界面退出时清掉的是它自己渲染的最后一行，而命令模式的进度条只有一行、正好就是那行。现在进度块末尾多留一个空行替它接刀，跑完的 100% 进度条会留在屏幕上，统计块从它下方开始
- **上传 / 下载的进度条不再提前冲到 100%**：它原先按「已传字节 ÷ 已开始节点的字节总量」算，并发受限时（如 50 台 10 并发）前 10 台传完就凑满分母、把进度顶到 100%，而另外 40 台还在排队；下一批开始后分子分母同时变大，进度又跌回来。现在按台数加权（已完成台数 + 各在传节点自身进度之和，除以总台数），进度只增不减，真实反映"还剩多少台没做完"
- **output 原文的行首缩进不再被吃掉**：`who -b` 这类靠行首空格对齐的输出（`system boot` 前那串空格），此前在终端、`output.txt`、`output.xlsx` 三处都被当作「首部空白」清掉——单行输出受害最明显，多行则只有第一行丢缩进，看起来像随机丢字符。现在整备只在采集侧做一次、只去掉整块**最前与最后的空白行**：行首缩进、中间的空行（`ls -l` / `df -h` 靠它分段）与行尾内容一律原样，终端与 `output.txt` 拿到什么就输出什么
- **写入 Excel 的文本逐处清理**（对位旧 `clean_for_excel`，output.xlsx 与 results.xlsx 两处都覆盖）：非法 UTF-8 替换 → 去 ANSI 颜色转义 → 去控制字符与零宽字符 → 统一换行 → 行首 `=` 前补空格（防被判成公式）。**清理覆盖每一个写入单元格的值**，包括最容易漏的「兜底分类＝错误原文」。远端输出仍**原样**交给终端与 output.txt（采集侧不做任何处理），脏字符只在落 xlsx 时清：旧版 openpyxl 遇到控制字符会直接抛错，Go 的 excelize 不报错、而是把非法字节逐个替换成 `�`，结果是单元格里留下 `�[31m…` 这种残缺垃圾——所以这一步不是可有可无，而是决定「表里是干净文字还是满地乱码」
- 危险命令警告框**框线对齐**：标题两侧留白不对称（左 2 空格、右 1 空格）已改对称；内容 / 分类改按**显示宽度**截断（原先按字符数截断，中文按 2 列算会把框线撑歪）
- **传输模式进度条不再整体下移**：进度块每重绘一次就向下漂移（并在上方留下同样多的空行），20+ 行时十几个来回就堆满屏幕、进度条被顶出可视区——现在擦除后原地重绘，锚点不再移动
- **rc 文件里的主密钥收敛为唯一一条**：此前只替换文件里第一条 `export` 行，而 shell 以**最后一条**赋值生效，rc 里存在两条时会出现「工具认为密钥是 X、终端实际用 Y」，症状是凭据解密报「主密钥不匹配」。现在写入时把主密钥行统一收敛到文件末尾；另一侧 shell 的 rc（如 zsh 的 `.zshrc`）若已存有主密钥，一并改写为同一把——两个 shell 各持不同密钥是同一类隐患，只在换 shell 后才爆发。不存在的 rc 文件不会被创建
- Windows 生成主密钥后的提示说清风险：当前终端仍在使用**旧**密钥，此刻执行 `--convert-password` 会用旧密钥加密文件，重开终端后就解不开了

## [5.0.1] - 2026-09-15

### Added

- 帮助信息首行下方显示版本号（版本号在入口文件单点维护），并带上编译来源时间戳（git 提交时间，编译时自动注入；仓库外编译时只显示版本号）

### Changed

- **执行期日志恢复旧版形态**：`SSHFleetExec.log` 从「无时间戳的结果明细原文」改回旧引擎那种带时间戳、带级别的节点运行日志——每台节点记录连接成功/失败（含耗时与原因）、执行/传输完成（含退出码或文件数）、分类，收尾记录连接统计与成败统计
- 工具日志在**执行开始时声明执行期日志的去向、执行结束后声明切回**，方便按路径找到本次运行的明细；声明轮转后工具日志不再夹带执行期内容（「开始执行任务」随轮转写入执行日志）
- 日志格式优化（工具日志与执行日志同样适用）：级别标签对齐（`[INFO   ]` / `[SUCCESS]`），分隔符 `-` 两侧各加两个空格
- 工具日志在每次**执行的开始与结束各写一行分界符**（缩进横线、无时间戳前缀），同一天多次执行的记录一眼可分
- **旧版 4.x 的 0x01 加密凭据不再支持读取**（复核审计后裁定：本次重写不考虑向后兼容）。识别到旧密文时会明确提示「不再支持」并给出处理指引——把明文密码重新写入凭据文件，再用 `--convert-password` 按当前等级重新转换。此前 5.0.0 为旧密文保留了只读解密

### Fixed

- **传输模式的节点进度条恢复旧版排队机制**：同时最多显示 20 条（对位旧版 `MAX_VISIBLE_NODES=20`），节点真正开始传输才获得显示位，完成一个释放一个——此前未传输的节点也占满一屏 0% 空条
- **传输模式进度块收缩后旧行残留**：节点条完成移除后，旧块多出的行不被擦除，统计结果打印时叠在半截进度条中间——现在整块擦除后从原位重绘
- 终端配色全面恢复为旧版方案：执行参数确认界面（横幅青、字段名亮青、值亮橙、确认问句亮黄、开始执行蓝）、`[Y/n]` 确认提示红、字段补全三处确认与上传并发建议/上传内容标题黄、危险命令警告框按风险红/黄/白、统计块（标签青、成功绿、失败红、失败分类统计红、分类与退出码提示黄）、`[ERROR]`/`[INFO]`/`[警告]` 前缀红/青/黄
- 命令模式执行时单条结果明细不再与进度条互相覆盖错位：结果明细改为从进度条**上方**滚动输出，进度条始终固定在底部（对齐旧版 rich Live 的上下分区观感），全部终端输出经同一把锁串行写；擦除进度条不再在结果块之间留下空行
- 危险命令确认环节回答「否」时，程序以**退出码 1** 结束（此前为 0，与脚本/自动化按退出码判断成败的习惯不符）
- 字段补全交互（用户名 / 密码）中取消（Ctrl+C 或输入流结束）会正常终止程序——此前会带着空凭据继续往下执行
- 下载模式的远程路径预检对含单引号的路径做了转义，不再拼出坏命令
- 失败分类里超长报错的截断改按**字符**进行，中文报错不再在截断处出现乱码

## [5.0.0] - 2026-09-14

从「Python 编排 + 独立 Go 引擎」重写为**单一可执行文件**。使用方式刻意保持不变——命令行选项与旧版完全一致，清单、凭据文件、历史记录的结构也仍然是你认得的那个样子。

### Changed

- **一个文件就是全部**：不再需要安装 Python，也不再需要另外准备一个引擎程序。Windows 一个 `SSHFleet.exe`、Linux 一个 `SSHFleet`，解压即可运行
- **配置文件改用 TOML**，位置为 `./config/SSHFleet.conf`。所有字段都必须写出来（允许留空），不再有藏在程序里的默认值——缺字段或取值不合法会在启动时直接指出是**哪一项**，不会跑到一半才发现
- **危险命令规则与错误分类关键词**改为同目录下的 `dangerous_keywords.toml`、`error_keywords.toml`。规则写法与内容不变（仍是「一条一条」的平级写法：名字 / 示例 / 正则 / 级别）
- **并发执行与进度显示都在工具内部完成**：不再有额外的引擎进程，也不再占用本地端口
- **历史记录目录**里的执行日志更名为 `SSHFleetExec.log`；工具日志仍是 `historys/SSHFleetTools.log` 这一个滚动文件
- **资源备份**（`assets/`）只备份清单与脚本，不再把上传的文件也复制一份
- **凭据加密格式升级**：老的密文仍可直接使用、无需重新转换；新转换出的密文采用更强的加密方式

### Added

- **密钥登录按 `-k` 三种用法明确分流**：不写 `-k` = 纯密码登录（清单里的密钥列一并忽略，不再出现「明明没用密钥、却因为密钥文件有问题而报错」）；只写 `-k` = 逐台使用清单/配置里的密钥；`-k 路径` = 所有服务器统一使用这把私钥（私钥有口令时在运行时手动输入）
- **私钥与其口令必须同源**：清单第 5/6 列成对、配置里 `key` 与 `key_passphrase` 成对、用 `-k 路径` 时口令手动输入；不会再出现口令从另一处兜底造成的错配
- **新增一种明确的失败分类**：同一台服务器同时提供了私钥与密码、且两者都被拒绝时，结果显示「密钥与密码均失败」，不再与「只配了密钥且密钥被拒」混为一谈。相应地，用私钥的服务器不会再被塞上配置里的默认密码——那份密码对它没用，出问题时只会干扰判断
- **常见退出码提示**：失败统计里出现 126 / 127 / 130 / 137 / 143 / 255 这类退出码时，直接标出含义（如 127 = 命令未找到、130 = 被中断）
- **配置预检查覆盖取值合法性**：端口范围、执行身份取值、超时是否为正整数、上传阈值之间的关系等，一次报清
- **Windows 上也能用 `latest_history` 快捷入口**：指向最新一次的历史目录，不需要管理员权限或开发者模式

### Fixed

- **上传目录时保留子目录层级**：此前目录里的所有文件会被平铺到目标目录、子目录一概丢失（不同子目录里的同名文件还会互相冲突，导致整台服务器上传失败）。现在内部层级原样重建，中间目录按需创建；上传目录自身那一层不重复出现（`-u /本地/abc -p /opt/` → `/opt/<abc 的内容>`）
- **参数误用不再被静默忽略**：写了通配符（如 `-u /本地/abc/*`，会被终端展开成多个路径）、路径含空格未加引号、参数打多或打错这三类情况，现在会直接报错并列出**未被使用的参数**——此前它们被悄悄丢掉，表现为「看起来成功、实际少传」
- **软链接不再被悄悄丢掉**：源里的软链接（含 Windows 目录联接与 `.lnk` 快捷方式）一律**过滤**、不中断执行。被过滤的条目写进该节点的结果明细（归档里的 `output.txt`、执行日志、`output.xlsx` 都能看到，形如 `link_a.txt: 已跳过（符号链接）`）；**上传时还会额外在终端提示**被过滤了哪几个。此前上传是静默跳过、下载则毫无痕迹；并且 Windows 目录联接会被误当成普通文件去传，导致那台服务器上传失败。只有过滤后一个可传文件都不剩时才报错
- 下载「一个文件都没取到」时的提示更贴合原因：目标全是软链接时明确说明已过滤多少个，不再报成「远程目录为空」
- 危险命令识别准确性大幅提升：`rm -rf /opt/test/123` 这类「删除指定目录」不再被误判成删除系统根目录而直接拦截；而 `rm -r -f /`、`rm -R -f /`、`rm --recursive --force /` 这些真正危险的写法此前反而只给提醒、甚至完全没有提示——现在按实际风险分类（实测语料准确率 62.1% → 100%）
- 非递归删除不再被当成递归删除：`rm -f 文件`、`rm -r 目录` 分别归入「强制删除」「递归删除」；系统目录与日志目录下的单文件删除也不再被判成删根
- 清理 5 条低信息量提醒（环境变量设置、命令别名设置、重启 Shell、文件描述符限制、内存限制）——它们只影响当前会话、没有破坏性，此前会频繁打断正常操作
- **不再改写你的脚本文件**：旧版在检查脚本时会把它就地转成 LF 换行（等于悄悄改了本地文件），现在只在上传时于内存中处理，本地文件保持原样
- 大目录上传/下载不再被自身的进度显示拖慢：逐文件进度与传输字节进度采用同一节流口径，传输速度不再受文件数量影响
- 凭据文件校验失败时提示统一为单一来源，不再出现两套措辞互相矛盾、甚至给出错误处理指引
- `secret_dir` 留空时的相对路径处理：明确提示「未配置 secret_dir」并给出两条出路，不再拼出错误路径后报「找不到凭据文件」
- 参数确认表格中含全角标点（如（）：）的标签对不齐的问题
- 汇总报告补上下载模式的执行参数（此前只有命令 / 脚本 / 上传三种模式有）
