# Changelog

所有重要变更记录在此。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

**面向用户**：本文档面向使用工具的产品用户，只记录每次版本更新中您可以**直接体验到的功能新增、体验优化与问题修复**。
描述使用用户语言：遇到了什么现象、现在能正常做什么、新增了什么能直接用的功能

以下几点明确不记录：
- 不出现函数名、变量名、模块名、文件名等内部代码符号（如 `clean_for_excel`、`format_output_to_xlsx`）
- 不记录代码实现细节、架构调整、重构拆分等内部变更
- 不记录测试、构建、工具链等开发侧变更

---

## [3.8.0] - 2026-08-07

### Added
- 新增 SSH 密钥登录：节点可配置 PEM 私钥（及私钥密码），在 CSV 第五列填写密钥文件路径，与密码列二选一
- 密钥优先、密码兜底：节点同时配置密钥和密码时优先用密钥，密钥解析失败且配置了密码则自动回退到密码认证
- 支持按节点单独设置私钥口令：在 CSV 第六列填写口令文件路径（内容为 Base64 编码的口令），留空则回退全局配置，方便不同节点使用不同加密私钥

### Fixed
- 修复仅依赖配置默认密码的旧 CSV（密码列留空）无法使用的问题（密码列留空现在正确回退到默认密码）
- 修复仅配置密钥、密码列留空的节点被误判需要默认密码、并错误触发交互输密码的问题

---

## [3.7.3] - 2026-08-06

### Fixed
- 修复上传小文件时进度条从 0 直接消失、看不到完成态的问题（小文件传输过快，完成态在删除前未被渲染，现在完成时强制刷新一次进度条）

---

## [3.7.2] - 2026-08-05

### Fixed
- 修复执行结果导出 Excel 时，远程命令带颜色等控制字符导致文件生成失败的问题
- 修复导出 Excel 异常时导致整个程序中断退出的问题（导出失败现在只提示，不影响主任务执行）

---

## [3.7.1] - 2026-07-31

### Fixed
- 修复密码文件路径使用相对路径时找不到密码文件的问题（相对路径现在会正确拼接基准目录，缺失时给出完整路径提示）
- 修复密码目录配置含 `~` 时路径无法识别的问题（`~` 现在会自动展开为用户主目录）
- 修复密码填相对路径但基准目录未配置时无明确提示的问题（现在加载配置时直接报错说明）
- 修复未指定连接超时参数时程序崩溃的问题（未指定时使用配置中的默认连接超时）
- 修复手动传入超时、并发数等参数被误判为格式错误的问题（这些参数现在统一按数字解析）
- 修复未指定执行模式参数时未生效配置中已设模式的问题（不传时默认使用配置的执行模式）
- 修复未指定并发数时并发数最终失效的问题（不传时默认 0，确认阶段自动替换为实际节点数）

---

## [3.7.0] - 2026-07-24

### Added
- 新增批量下载功能（`-d` 参数），支持通过 SFTP 将远端文件/目录批量下载到本地，与命令、上传、脚本等模式互斥
- 下载进度条显示，复用上传的进度框架
- 下载参数自动校验：远端路径需为绝对路径、本地目录需已存在
- 帮助信息新增下载模式示例

### Changed
- 下载成功统一归入"传输成功"分类统计

---

## [0.6.0] - 2026-07-24

### Added
- 密码存储改造：密码列从明文改为引用加密的密码文件路径，提升安全性
- 新增密码目录配置项，支持相对路径与 `~` 解析
- 新增执行前统一预检查密码文件机制，提前暴露错误
- 新增 `-f` 参数支持直接内联 CSV 文本传入
- 新增非交互模式（`--disinteractive`），自动跳过所有确认提示，便于脚本化调用
- 新增并发数提示，执行前给出风险警告
- 上传模式新增输出文件写入（命令模式和上传模式共用输出记录）
- 上传进度条新增成功/失败实时计数

### Changed
- CSV 密码列语义从「明文密码」改为「密码文件路径」
- 非交互模式支持跳过确认
- `-f` 参数文件不存在时支持作为内联文本传入
- 帮助信息格式优化，支持中文字符对齐
- 未指定并发数时从"提醒"改为"默认约束"，避免误用过高并发
- 帮助信息修正并发数描述、精简各参数说明

### Fixed
- 修复命令模式输出文件创建但内容为空的问
- 修复上传模式未生成输出文件的问题
- 修复连接失败时成功计数错误的问题
- 修复密码文件解码错误的问题
- 修复 SSH 连接耗时统计与错误处理
- 修复多文件上传进度累计异常、速度为负的问题
- 修复上传进度显示的是实际上传字节而非总字节的问题
- 修复 `--nobash` 模式仍预处理命令的问题（现在完全跳过所有预处理）

---

## [0.5.0] - 2026-07-18

### Added
- 新增上传进度跟踪
- 新增健康检查，便于外部探活
- 任务不受客户端断开影响，长任务更稳定

### Changed
- 并发输出更安全，避免多节点结果错乱
- 上传使用 sudo 创建临时目录，解决无权限导致上传失败的问题
- 精简上传过程日志噪音

### Fixed
- 修复上传进度条不显示的问题
- 修复密码文件延迟到使用时才校验的问题

---

## [0.4.0] - 2026-07-15

### Added
- 新增环境变量支持与进程认证机制，提升安全性
- 新增接口文档，说明请求规范
- 新增更详细的运行日志（连接统计、请求拒绝、启动参数等）

### Changed
- 统一结构化日志格式
- 移除请求体预览，避免密码泄露风险

---

## [0.3.0] - 2026-07-10

### Added
- 新增文件上传功能，支持批量上传
- 新增上传并发阈值配置
- 新增无请求自动退出机制

---

## [0.2.0] - 2026-07-05

### Added
- 新增 SSH 任务输出记录功能
- 新增服务器关闭信号支持
- 新增日志路径有效性检查

### Changed
- 配置文件迁移至 YAML 格式
- 项目正式命名为 SSHFleet

### Fixed
- 修复超时参数校验逻辑
- 修复提示中的名称拼写
- 修复 Linux 环境下编码兼容问题

---

## [0.1.0] - 2026-07-01

### Added
- SSHFleet 初始版本发布
- 支持 SSH 连接管理与命令批量执行
- 支持执行结果导出 Excel
- 支持参数解析与帮助信息
