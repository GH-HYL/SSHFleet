# Changelog

所有重要变更记录在此。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

**面向用户**：本文档面向使用工具的产品用户，只记录每次版本更新中您可以**直接体验到的功能新增、体验优化与问题修复**。
描述使用用户语言：遇到了什么现象、现在能正常做什么、新增了什么能直接用的功能
更新记录以日期为维度进行汇总，同一天的所有变更将整合在同一个版本号下呈现。

**版本号要求**：
版本格式：主版本号.次版本号.修订号，版本号递增规则如下：
主版本号：当做了不兼容的 API 修改，
次版本号：当做了向下兼容的功能性新增，
修订号：当做了向下兼容的问题修正。



以下几点明确不记录：
- 不出现函数名、变量名、模块名、文件名等内部代码符号（如 `clean_for_excel`、`format_output_to_xlsx`）
- 不记录代码实现细节、架构调整、重构拆分等内部变更
- 不记录测试、构建、工具链等开发侧变更

---

## [3.8.4] - 2026-08-11

### Fixed
- 修复远程执行命令或脚本时，部分目标系统提示"找不到命令"（如 ntpq、systemctl 等系统管理命令）的问题：此前命令与脚本在精简环境中执行，手动登录时才有的完整命令路径未能加载；现在统一以登录环境执行，自动加载完整环境变量，与手动登录的效果一致
- 修复命令拼接日志里引号转义过多、可读性差的问题：此前完整命令中会出现多层 `'"'"'` 转义套娃，原因是命令里嵌套了多层带单引号的命令字符串；现在改用 base64 通道且内层完全裸写无引号，最终命令只有最外层一层单引号包裹，日志清爽可读

### Changed
- 精简执行过程中的日志输出：此前每个节点正常执行时会重复打印"连接成功、会话成功、节点输出"等多条日志，现在统一合并为一条"执行结束"日志，执行过程不再刷屏；连接或命令出错时的详细异常日志仍完整保留
- 命令拼接日志同时展示原始命令与最终执行命令：之前命令经过处理后日志里只能看到编码后的内容，现在能一眼对照"原来输入了什么、最终执行的是什么"

---

## [3.8.3] - 2026-08-10

### Added
- 新增 `-k` 密钥登录开关（三态）：不写 `-k` 时工具只走纯密码登录，并忽略清单/配置里的所有私钥与口令配置、不做密钥文件预检查；仅写 `-k`（不带路径）时按原逻辑用每个节点自己的密钥（清单第5列 > 配置默认）；写 `-k 私钥路径` 时把指定私钥作为所有节点的统一私钥，覆盖各自的密钥/口令配置，加密私钥会在运行时交互提示输入口令、直接回车表示无口令。密钥登录从此从「配了就默认用」改为「显式指定才用」

### Fixed
- 危险命令检测规则修正若干漏报与重复：现在能识别 `rm -fr` 顺序的强制删除；`chmod 777`、`chmod 7777` 等权限设置会被正确判定为风险；`chown` 修改文件所有者为 root（含 `root:组`、`组` 写法）均会告警；合并了重复的关机（shutdown）与屏蔽服务（systemctl mask）规则，避免同一条命令重复触发多条告警

---

## [3.8.0] - 2026-08-07

### Added
- 新增 SSH 密钥登录：节点可配置 PEM 私钥（及私钥密码），在 CSV 第五列填写密钥文件路径，与密码列二选一
- 密钥优先、密码兜底：节点同时配置密钥和密码时优先用密钥，密钥解析失败且配置了密码则自动回退到密码认证
- 支持按节点单独设置私钥口令：在 CSV 第六列填写口令文件路径（内容为 Base64 编码的口令），留空则回退全局配置，方便不同节点使用不同加密私钥

### Changed
- 连接日志（成功与失败）现在会标明本次使用的登录方式（密码 / 密钥），连接失败报错同样标注，便于排查是哪类认证方式导致的问题
- 危险命令检测规则、错误分类关键词两个配置文件由 JSON 改为 YAML 格式（文件名变为 `dangerous_keywords.yaml` / `error_keywords.yaml`）：现在可以直接用 `#` 注释掉整条规则或某个关键词，正则里的反斜杠（`\`）也不用再写成双斜杠，阅读和手动调整更轻松
- 如需自定义规则：直接编辑这两个 YAML 文件即可，原有规则内容已完整保留，分类与风险等级（禁止 / 高 / 中 / 低）都整理为带标题的分组，更易查找

### Fixed
- 修复仅依赖配置默认密码的旧 CSV（密码列留空）无法使用的问题（密码列留空现在正确回退到默认密码）
- 修复仅配置密钥、密码列留空的节点被误判需要默认密码、并错误触发交互输密码的问题
- 修复交互模式下，节点清单中端口/用户名/密码留空且配置也未设默认值时程序意外崩溃的问题；现在会正常提示你补全这些信息，不再中断

### Documentation
- 重写 README 的 CSV 文件格式说明：改为面向新手的端到端教程，补充密码文件 Base64 生成命令（Windows / Linux）、四种常见场景可直接照抄的示例，并修正此前「第六列会被忽略」与实现矛盾的描述
- 优化命令行 `--help` 与各参数说明文案（新手友好：解释五种模式、并发数含义、执行身份 direct/sudo 等），修正 `-n` 并发数默认值的错误描述；同步更新 README「参数说明」「快速开始」「配置文件」「常见问题」表述（补充最小可用配置示例，新增「第4列填什么」「怎么用密钥登录」两条 FAQ）
- 配置文件新增「默认私钥路径」`account.key`：所有节点共用同一个私钥时，只需在此配一次，CSV 第五列（私钥）留空即自动回退到它（与默认密码用法一致）；README「配置文件」「CSV 格式」同步补充该说明
- 配置文件 `account.password_dir` 重命名为 `account.secret_dir`：该目录实际同时存放密码 / 私钥 / 私钥口令三类凭据文件，旧名容易误以为只放密码；**如你已自定义过该配置项，请将 `password_dir` 改名为 `secret_dir`**（README 与配置示例已同步更新）

---

## [3.7.3] - 2026-08-06

### Fixed
- 修复上传小文件时进度条从 0 直接消失、看不到完成态的问题（小文件传输过快，完成态在删除前未被渲染，现在完成时强制刷新一次进度条）

---

## [3.7.2] - 2026-08-05

### Fixed
- 修复执行结果导出 Excel 时，远程命令带颜色等控制字符导致文件生成失败的问题
- 修复导出 Excel 异常时导致整个程序中断退出的问题（导出失败现在只提示，不影响主任务执行）

---

## [3.7.1] - 2026-07-31

### Fixed
- 修复密码文件路径使用相对路径时找不到密码文件的问题（相对路径现在会正确拼接基准目录，缺失时给出完整路径提示）
- 修复密码目录配置含 `~` 时路径无法识别的问题（`~` 现在会自动展开为用户主目录）
- 修复密码填相对路径但基准目录未配置时无明确提示的问题（现在加载配置时直接报错说明）
- 修复未指定连接超时参数时程序崩溃的问题（未指定时使用配置中的默认连接超时）
- 修复手动传入超时、并发数等参数被误判为格式错误的问题（这些参数现在统一按数字解析）
- 修复未指定执行模式参数时未生效配置中已设模式的问题（不传时默认使用配置的执行模式）
- 修复未指定并发数时并发数最终失效的问题（不传时默认 0，确认阶段自动替换为实际节点数）

---

## [3.7.0] - 2026-07-24

### Added
- 新增批量下载功能（`-d` 参数），支持通过 SFTP 将远端文件/目录批量下载到本地，与命令、上传、脚本等模式互斥
- 下载进度条显示，复用上传的进度框架
- 下载参数自动校验：远端路径需为绝对路径、本地目录需已存在
- 帮助信息新增下载模式示例

### Changed
- 下载成功统一归入"传输成功"分类统计

---

## [0.6.0] - 2026-07-24

### Added
- 密码存储改造：密码列从明文改为引用加密的密码文件路径，提升安全性
- 新增密码目录配置项，支持相对路径与 `~` 解析
- 新增执行前统一预检查密码文件机制，提前暴露错误
- 新增 `-f` 参数支持直接内联 CSV 文本传入
- 新增非交互模式（`--disinteractive`），自动跳过所有确认提示，便于脚本化调用
- 新增并发数提示，执行前给出风险警告
- 上传模式新增输出文件写入（命令模式和上传模式共用输出记录）
- 上传进度条新增成功/失败实时计数

### Changed
- CSV 密码列语义从「明文密码」改为「密码文件路径」
- 非交互模式支持跳过确认
- `-f` 参数文件不存在时支持作为内联文本传入
- 帮助信息格式优化，支持中文字符对齐
- 未指定并发数时从"提醒"改为"默认约束"，避免误用过高并发
- 帮助信息修正并发数描述、精简各参数说明

### Fixed
- 修复命令模式输出文件创建但内容为空的问
- 修复上传模式未生成输出文件的问题
- 修复连接失败时成功计数错误的问题
- 修复密码文件解码错误的问题
- 修复 SSH 连接耗时统计与错误处理
- 修复多文件上传进度累计异常、速度为负的问题
- 修复上传进度显示的是实际上传字节而非总字节的问题
- 修复 `--nobash` 模式仍预处理命令的问题（现在完全跳过所有预处理）

---

## [0.5.0] - 2026-07-18

### Added
- 新增上传进度跟踪
- 新增健康检查，便于外部探活
- 任务不受客户端断开影响，长任务更稳定

### Changed
- 并发输出更安全，避免多节点结果错乱
- 上传使用 sudo 创建临时目录，解决无权限导致上传失败的问题
- 精简上传过程日志噪音

### Fixed
- 修复上传进度条不显示的问题
- 修复密码文件延迟到使用时才校验的问题

---

## [0.4.0] - 2026-07-15

### Added
- 新增环境变量支持与进程认证机制，提升安全性
- 新增接口文档，说明请求规范
- 新增更详细的运行日志（连接统计、请求拒绝、启动参数等）

### Changed
- 统一结构化日志格式
- 移除请求体预览，避免密码泄露风险

---

## [0.3.0] - 2026-07-10

### Added
- 新增文件上传功能，支持批量上传
- 新增上传并发阈值配置
- 新增无请求自动退出机制

---

## [0.2.0] - 2026-07-05

### Added
- 新增 SSH 任务输出记录功能
- 新增服务器关闭信号支持
- 新增日志路径有效性检查

### Changed
- 配置文件迁移至 YAML 格式
- 项目正式命名为 SSHFleet

### Fixed
- 修复超时参数校验逻辑
- 修复提示中的名称拼写
- 修复 Linux 环境下编码兼容问题

---

## [0.1.0] - 2026-07-01

### Added
- SSHFleet 初始版本发布
- 支持 SSH 连接管理与命令批量执行
- 支持执行结果导出 Excel
- 支持参数解析与帮助信息
