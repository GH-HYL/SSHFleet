# SSHFleet 工作区忽略规则（白名单）
#
# 本仓库将对外推送，只保留「代码 + 决策文档」两类内容。
# 工作区会不断产生过程文件（草稿、临时数据、中间产物），用黑名单要反复维护；
# 因此改为白名单：默认全部忽略，只明确列出需要跟踪的几处。
# 需要新增追踪时，在下面补一行即可。
#
# 代价是「没列到的会静默掉队」——新增目录时记得回来看看要不要加。

# 忽略所有
*

# 忽略规则自身
!.gitignore
!.gitattributes

# 文档（ADR、规格、词表）
!CONTEXT.md
!AGENTS.md
!README.md
!CHANGELOG.md
!docs/
!docs/**

# 代码
!modules/
!modules/**

# 构建脚本（build-windows.bat / build-linux.sh）为个人使用工具，不进版本控制——
# 他人按 README 的 go build 自行编译即可

# 以下几类在 modules/ 里天然存在、但都不进版本控制。
# 注意：放在 !modules/** 之后才能生效——白名单里后写的规则覆盖先写的。

# 旧工程参考副本（只读）。用 * 通配，避免每换一次参考副本就回来改这条
modules/*_bak*/

# 运行期日志落点（工具自己生成的历史记录，属产物）
historys/

# 构建产物兜底排除：!modules/** 会连带放行它，而历史上 build-linux.sh 相对路径写错时
# 产物曾落到这里并被误入库（2026-09-14 修脚本 + 补本条）
modules/build/

# ---- 以下为个人使用内容，刻意不纳入追踪（白名单已天然排除，此处仅作说明）----
#   个人开发规范.md               个人规范文件
#   审计交接.md                    本地过渡文档（重构完成后即失效，不进对外仓库）
#   tools/                        辅助脚本、离线依赖包等（全部不纳入）
#   test/                         本地测试语料与样本
#   build/  release/              构建中间产物与发布产物
#   build-windows.bat             构建脚本（个人使用，不进版本控制）
#   build-linux.sh                构建脚本（个人使用，不进版本控制）
#   根目录临时脚本（.bat 等）      按需补白名单
#
# 另：modules/ 下按上述规则排除 historys/ 与 *_bak*/，其余一律追踪。
