# SSHFleet 工作区忽略规则（白名单）
#
# 本仓库将对外推送，只保留「代码 + 决策文档」两类内容。
# 工作区会不断产生过程文件（草稿、临时数据、中间产物），用黑名单要反复维护；
# 因此改为白名单：默认全部忽略，只明确列出需要跟踪的几处。
# 需要新增追踪时，在下面补一行即可。
#
# 代价是「没列到的会静默掉队」——新增目录时记得回来看看要不要加。

# 忽略所有
*

# 忽略规则自身
!.gitignore

# 文档（ADR 与规格）
!CONTEXT.md
!AGENTS.md
!README.md
!CHANGELOG.md
!docs/
!docs/**

# 代码
!modules/
!modules/**

# 旧工程参考副本（只读，不进版本控制）
modules/SSHFleet_bak/

# ---- 以下为个人使用内容，刻意不纳入追踪（白名单已天然排除，此处仅作说明）----
#   个人开发规范.md   个人规范文件
#   tools/            辅助脚本与打包工具
#   test/             本地测试语料与样本
#   根目录临时脚本（.bat 等）按需在上方补 `!文件名`
