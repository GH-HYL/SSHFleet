# SSHFleet 工作区忽略规则
#
# 采用「黑名单」写法，只忽略该忽略的。
# 不用「忽略所有 + 白名单」——旧仓库正是那种写法，导致 test/ 与 tools/ 从未进入
# 版本控制，清理时只能依赖手工备份恢复。别在新仓库重蹈。

# 旧工程参考副本（只读，不入新仓库）
modules/SSHFleet_bak/

# 构建中间产物与最终产物（见 个人开发规范.md §一）
build/
release/

# Go 编译产物
*.exe
*.test
*.out
