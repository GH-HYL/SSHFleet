# M5-20 output：终端呈现（结果明细 / 统计块 / 进度条）

Type: task
Status: resolved
Resolved: 2026-09-14
Blocked by: 19

## 范围

`internal/output` 终端部分（对位旧 `terminal.py`，rich → 自写 ANSI 渲染）：

- **结果明细**：`【IP】 连接: 成功 - X.XXXs` / `【IP】 执行|上传|下载: …` / 输出内容 / `【IP】 分类: …` / 分隔线；命令模式打印到终端，所有模式都落 `output.txt`
- **统计块**：总耗时、节点总数/完成总数/总数校验、成功失败、失败分类（已分类合并一行、**兜底原文独占一行**）、常见退出码提示（8 条映射，按台数降序）
- **进度条复刻旧 rich 观感**（用户 2026-09-14 要求「尽量复刻」，验收通过）：40 格条形图（完成绿 / 结束蓝 / 节点青 + 暗色未完成）、百分比右对齐、速度（2 秒滑动窗口）、字节「已传/总量」列；传输模式＝总进度 + 节点完成进度 + 50 格分隔线 + 逐节点明细（`IP Total/Succ/Fail`）；命令模式＝单行「执行进度 已完成 N/M」+ Succ/Fail
- **差异**：删掉 20 节点显示上限与排队机制（rich 多进度条的限制逼出来的），改为按终端高度自适应；光标上移重绘自写
- 注入接缝不变：`main` 把 `ProgressUI.Update` 作为 `batch.Hooks.OnProgress` 注入

## 验证

- 真机冒烟：命令模式进度条（`^[[1A` 重绘 + 绿/蓝配色）→ 明细 → 统计块；传输模式逐节点明细行（`0% 0B/s IP Total:1 Succ:0 Fail:0`）
- 统计块兜底原文与退出码提示逻辑随 M4 语料间接覆盖

## Comments

- 2026-09-14 完成。
