# M2-11 confirm：参数确认交互

Type: task
Status: resolved
Resolved: 2026-09-11
Blocked by: 10

## 范围

`internal/confirm`：

- 上传并发建议：目录/文件大小计算（排除符号链接）→ 阈值三分（small 全并发不提示 / large 串行 / medium 建议值）→ y 应用、n 保留原值
- `-n` 缺省以节点数落定（沿用第 6 步后的 0 语义）
- 信息表（权限类型/执行模式/路径/超时/备注）：排版对齐换 `lipgloss.Width`，横幅与文案保持观感一致
- 上传内容树（一层 `└──`）照旧
- 非交互模式：显式打印「跳过确认直接执行」并视为已确认（不再借 yorn 值，spec 实现层差异）
- 取消（N）→ 提示「操作已取消」+ 日志 warning + 以 1 退出

## 验证

- 冒烟：信息表输出、非交互跳过、取消退出码

## Comments

- 2026-09-11 完成。临时测试 4 例全过（跑完已删）：中间档建议并发、大文件串行、小文件不建议并落节点数、显式 -n 不被覆盖。信息表对齐改用 `lipgloss.Width`。
