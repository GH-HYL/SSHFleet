# M6-27 CHANGELOG 从 5.0.0 起

Type: task
Status: resolved
Resolved: 2026-09-14
Blocked by: 26

## 范围

仓库根 `CHANGELOG.md`（此前不存在）。

按 `个人开发规范.md` 第四节执行：只记用户能直接体验到的变化，不记内部代码符号、实现细节与重构拆分、测试与构建工具链。

**定版（用户 2026-09-14 裁定）**：直接写 `## [5.0.0] - 2026-09-14`，不走「待定」。
依据：规范要求「至少次日后才可定版」，而本次重写的起始日期是 2026-09-11（首次提交 `4337fb1`），距今 3 天，满足条件。

内容基线是**旧版 4.2.4**（旧仓库最后一个正式版本）。旧仓库 CHANGELOG 顶部那条 `## [待定] - 2026-09-10` 记录的危险命令改造从未发布成正式版本，因此并入 5.0.0 的「Fixed」，不丢记录。

段落划分：

- **Changed**：单可执行文件、配置改 TOML 且字段全必填、规则文件改 TOML、并发与进度不再需要引擎进程与端口、执行日志更名 `SSHFleetExec.log`、`assets/` 不再备份上传文件、凭据加密格式升级（旧密文仍可读）
- **Added**：`-k` 三态分流、私钥与口令同源、新分类「密钥与密码均失败」、常见退出码提示、配置取值合法性预检查、Windows 上的 `latest_history`
- **Fixed**：危险命令识别准确性（62.1% → 100%）、非递归删除不再被当成递归删除、清理 5 条低信息量提醒、**不再改写本地脚本文件**（旧版检查脚本时会就地转 LF）、大目录传输不再被进度显示拖慢、凭据校验提示统一、`secret_dir` 留空时的相对路径处理、全角标点导致的对齐问题、报告补下载模式参数

## 验证

- 校验「只记用户可感」这条：逐条检查没有出现内部符号、模块名、测试与构建工具链字样
- 版本号可被打包脚本提取（两个构建脚本的 release 模式都从本文件顶部取版本号，实测取到 `5.0.0`）
- 与旧仓库 CHANGELOG 的 4.2.4 及之前的条目比对，确认没有把已发布过的 4.2.x 修复重复算进 5.0.0

## Comments

- 2026-09-14 完成。文件顶部留了一行说明：「本文件自 5.0.0 起记录；更早的版本记录属于上一套实现，不在本文件内」——避免读者以为历史被删了。
