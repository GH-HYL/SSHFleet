# SSHFleet 全 Go 重写 · 规划覆盖面

Status: needs-info

> 本文件定义**规划要覆盖哪些面**，以及每一期怎么按面推进。
> 目的：防止"拆东补西"——盯着一段旧代码改得挺好，回头发现另一个面上有个老行为没接住。

---

## 一、为什么要有这份文件

重构最容易出的问题不是写不出来，是**漏面**。旧代码两年攒下来的行为分布在四个维度上（配置、清单、凭据、规则、输出、边界），任何一处只靠记忆去覆盖，都会漏。

所以：**每个里程碑动工前，先对着这份名单过一遍本期的面**，把要回答的问题列出来，再动工。不跳步、不凭印象。

---

## 二、十个覆盖面

| # | 面 | 主问题 | 覆盖内容 | 产出 | 时点 |
| --- | --- | --- | --- | --- | --- |
| F1 | **功能面** | 这个用户可见功能还要不要？要的话行为变不变？ | 四执行模式（命令/脚本/上传/下载）、两工具模式（生成主密钥/转换凭据）、清单与字段补全、凭据三等级、密钥三态、危险检测、错误分类、参数确认、进度呈现、归档产物、并发与超时、执行身份 | 逐功能的裁定结论 | 贯穿 M1–M5 |
| F2 | **结构面** | 这段逻辑属于主干哪一步？放哪个目录？函数怎么分层？ | `main.go` 主干十步、11 个 `internal/` 目录的职责边界、`internal/common` 的进出规则、单文件过大时的功能边界拆分 | 骨架定稿 + 每目录职责说明 | M1 起；每期动工前先定归属 |
| F3 | **数据面** | 工具读写的一切文件：格式、字段、路径、兼容性 | 主配置（YAML→TOML）、危险规则文件（→TOML）、错误关键词（→TOML）、节点清单（CSV / 内联）、凭据文件（三等级 + 新旧密文格式）、主密钥环境变量、历史归档产物结构 | schema 定稿 + 迁移说明 | M1 / M2 / M4 / M5 |
| F4 | **接口面** | 外部怎么调用、看到什么、依赖什么 | 命令行选项表（含模式互斥与提示文案）、退出码、stdin/stdout/stderr 行为、环境变量、文件系统副作用 | CLI 定稿表 | M1 |
| F5 | **行为面** | 出错时怎样？边界在哪？与旧版是否一致？ | 退出码语义、预处理失败 / 传输失败、错误分类、危险等级与拦截、超时、中断（Ctrl+C）、部分成功、空清单 / 空目录 / 符号链接 / 权限不足 | 行为对照结论（有出入必须 ask） | M3 / M4 |
| F6 | **构建面** | 从源码到可运行产物 | 依赖与版本、Windows / Linux 交叉编译、产物命名与落位（`release/`）、构建脚本（`tools/`）、是否静态链接 | 构建方案 | M6 |
| F7 | **文档面** | 仓库里该有的文字，给谁看 | `CONTEXT.md` 词表、`docs/adr/`、README（用户手册）、`CHANGELOG.md`（5.0.0 起）、`AGENTS.md`、配置迁移说明 | 文档重写清单 | ADR 随决策随时写；其余 M6 |
| F8 | **迁移面** | 用户既有资产怎么过渡 | 配置文件、凭据文件、既有调用方式、旧 `historys/` 目录是否复用 | 迁移说明 | M6 |
| F9 | **验证面** | 拿什么对照才算做对 | 危险检测语料、错误分类语料、真机跑通、与旧版行为逐项对照 | 每期出口条件 | 各期出口 |
| F10 | **风险面** | 已知的坑在哪 | ① shell 解析层行为漂移 ② 并发 + 进度聚合的时序 ③ SFTP 大文件与目录递归 ④ Windows/Linux 路径差异（`~` 展开、软链） ⑤ 凭据新旧格式并存 ⑥ xlsx 库行为差异 | 风险清单 + 消解时点 | M1 识别；各期逐个消解 |

---

## 三、每个里程碑必过的面

| 里程碑 | 必过面 | 出口条件 |
| --- | --- | --- |
| M1 框架层 | 结构面、接口面、数据面（配置）、风险面（识别） | 主干十步成型；CLI 与配置可用 |
| M2 数据层 | 数据面（清单 / 凭据）、行为面（字段补全 / 输入记忆）、结构面 | 能产出完整的节点信息与凭据 |
| M3 执行层 | 行为面（超时 / 中断 / 并发 / 部分成功）、风险面（SFTP / 路径差异）、结构面 | 可对真机批量执行 |
| M4 判定层 | 功能面（危险检测 / 错误分类）、行为面（等级 / 退出码）、数据面（规则文件）、验证面 | 危险检测与错误分类对旧语料结果一致 |
| M5 输出层 | 数据面（归档结构）、功能面（呈现 / 报告 / xlsx）、结构面 | 归档结构与旧版对齐 |
| M6 收尾 | 构建面、文档面、迁移面 | 5.0.0 可发布 |

---

## 四、每期动工的规程

1. **先过面**：把本期应覆盖的面逐条列出，写成**问题清单**（此时不碰代码）。
2. **以旧代码为事实来源**：每个问题都去旧实现里找答案，不凭印象、不凭这份文件里的摘要（D20）。
3. **三问**：① 能不能优化 → ② 优化后是否**保持原功能** → ③ 保持不了、或确有更优方案 → **ask 用户裁定**。
4. **结论三种归宿**：

   | 结论性质 | 归到哪 |
   | --- | --- |
   | 定下的决策 | `spec.md` 的决策表 |
   | 难回退 + 不看背景会疑惑 + 真有权衡 | 新写一条 ADR（`docs/adr/`） |
   | 术语 | `CONTEXT.md` 词表 |

5. **不确定就挂待定**：写进 `spec.md` 的「待定」段，不靠猜往下推。

---

## 五、当前各方面的状态

| 面 | 状态 |
| --- | --- |
| F1 功能面 | 未开始——随各期逐个裁定 |
| F2 结构面 | 骨架已定稿（主干十步 + 11 目录） |
| F3 数据面 | 主配置草案已出；规则文件格式已定；清单 / 凭据 / 归档待各期 |
| F4 接口面 | CLI 形态已定（平级选项）；选项总表待细排 |
| F5 行为面 | 未开始 |
| F6 构建面 | 未开始 |
| F7 文档面 | 词表已建；ADR 待决策产生 |
| F8 迁移面 | 未开始 |
| F9 验证面 | 语料已恢复（危险检测 223 行 / 错误分类 205 行 + 清单样本） |
| F10 风险面 | 已识别 6 项，均未消解 |
