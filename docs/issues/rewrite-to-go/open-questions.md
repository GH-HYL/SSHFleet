# 待裁定问题

Status: needs-info

> 只列**推荐与旧行为不同**、且需要用户裁定的问题。
> 推荐与旧代码一致的，不在此记录——重构时照旧代码实现（见 D20）。
> 已裁定的差异决策进 `spec.md`；值得留痕的权衡写成 ADR。

---

## 当前前沿：空

六期（M1–M6）的决策点已全部提完并裁定。已裁定的差异决策见 `spec.md` 第四节（行为差异）与第二、三节（架构 / 选型）。

非裁定类待定项见 `spec.md` 第十节。

---

## 当前前沿：M2 实施中记录的待回复项（2026-09-11）

用户外出期间按「先记录、先实施」处理：以下各项均已按**暂定实现**落地，回来裁定后如需改动再调整。

### Q4（Q3 遗留下半）：密钥节点要不要套用配置默认密码？

- 旧代码档位顺序：清单密码列 > **配置默认密码** > 密钥认证（有私钥则空）> 记忆 > 交互
  —— 即节点有私钥、清单密码列空时，只要 `account.password` 配了值，仍会套用默认密码；且默认密码文件损坏会直接退出，哪怕该密码对密钥登录无用。
- 关联事实（用户问「私钥无口令时带着密码连接会怎样」）：旧 Go 引擎 `BuildAuthMethods` 是**密钥优先、密码 fallback**；密钥认证成功则密码根本不会被发送。只有当密钥被拒绝时才回退尝试密码——此时多余的默认密码可能触发失败次数累计/锁定与日志噪音，也会掩盖「密钥失效」这个事实。
- **暂定实现**：密钥认证优先（有私钥 → 密码置空），配置默认密码只给「清单密码列空、又无私钥」的节点兜底。

### Q5：`execution.mode` 要不要给默认值？

- 旧模板注释写「默认 sudo」，但 pydantic 模型里该字段必填（不写即报错）。
- spec D6 只给超时值与开关类默认值，mode 未列入 → **暂定实现**：保持必填，不写 mode 报「配置缺少必填字段」。

### Q6：校验顺序（关键词文件存在性 vs 参数合规检查）

- 与旧一致：先查关键词文件是否存在，再查参数合规。故 `-c` `-s` 同给且关键词文件缺失时，先报「配置文件缺失」而非「模式互斥」。
- **暂定实现**：保持旧顺序。

---

## ADR

| ADR | 主题 |
| --- | --- |
| `docs/adr/0001-skip-host-key-verification.md` | 跳过 SSH 主机密钥校验（数千台规模，维护 `known_hosts` 不现实） |
| `docs/adr/0002-upload-p-requires-trailing-slash.md` | 上传 `-p` 一律要求绝对路径且以 `/` 结尾（消歧义约束，非冗余校验） |

两条 ADR 记录的**都是与旧代码一致的决策**，但**理由不在旧代码里**（旧代码只是那么做了）——正是 ADR 该留痕的情形。
