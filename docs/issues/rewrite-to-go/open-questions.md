# 待裁定问题

Status: needs-info

> 只列**推荐与旧行为不同**、且需要用户裁定的问题。
> 推荐与旧代码一致的，不在此记录——重构时照旧代码实现（见 D20）。
> 已裁定的差异决策进 `spec.md`；值得留痕的权衡写成 ADR。

---

## 当前前沿：空

六期（M1–M6）的决策点已全部提完并裁定。已裁定的差异决策见 `spec.md` 第四节（行为差异）与第二、三节（架构 / 选型）。

非裁定类待定项见 `spec.md` 第十节。

---

## ADR

| ADR | 主题 |
| --- | --- |
| `docs/adr/0001-skip-host-key-verification.md` | 跳过 SSH 主机密钥校验（数千台规模，维护 `known_hosts` 不现实） |
| `docs/adr/0002-upload-p-requires-trailing-slash.md` | 上传 `-p` 一律要求绝对路径且以 `/` 结尾（消歧义约束，非冗余校验） |

两条 ADR 记录的**都是与旧代码一致的决策**，但**理由不在旧代码里**（旧代码只是那么做了）——正是 ADR 该留痕的情形。
