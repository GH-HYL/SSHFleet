# CONTEXT.md

SSHFleet 工程领域上下文（单一上下文布局）。

**本文件是词表，只定义术语，不承载实现细节、不放规格。**

---

## 词汇表（Glossary）

| 术语 | 定义 |
| ---- | ---- |
| 节点清单（node list） | 描述目标主机集合的清单。两种来源：CSV 文件路径，或 `-f` 直接给出一段内联信息 |
| 节点信息（node info） | 单台目标主机的连接要素：`ip` / `port` / `user` / `password` / `key_content` / `key_passphrase` |
| 凭据（credential） | 密码、私钥、私钥口令三类认证材料。清单第 4 / 5 / 6 列分别对应其路径或内容 |
| 凭据目录（secret dir） | 由配置 `secret_dir` 指定的目录。清单与配置中书写**相对路径**的凭据文件，一律拼接至该目录 |
| 密码安全等级（password security level） | 凭据文件在磁盘上的存储形态分级：`1`=明文、`2`=base64 编码、`3`=加密；数字越大越安全 |
| 主密钥（master key） | 随机密钥。持久化于 shell 配置文件（`~/.bashrc` / `~/.zshrc`）或 Windows 注册表，运行期经环境变量 `SSHFLEET_KEY` 读取。等级 `3` 下解密凭据文件的唯一依据，与凭据文件分处不同存储位置 |
| 凭据转换（credential conversion） | 按当前密码安全等级把凭据文件**就地**转换为对应格式的操作。已完成目标格式的文件跳过；私钥 PEM 不参与转换 |
| 密钥三态（key mode） | 密钥登录的三种启用状态：`off`（未指定 `-k`，强制空密钥，清单第 5/6 列整体忽略）、`default`（`-k` 无路径，逐节点按「清单 > 配置」解析密钥）、`universal`（`-k` 带路径，强制统一使用命令行私钥，忽略节点自带密钥/口令）。私钥口令与私钥**同源**取用 |
| 字段补全（field resolution） | 单节点各字段按固定优先级取值的过程：清单列 > 配置默认 > 输入记忆 > 交互输入。**密码的例外**：该节点最终持有私钥时，配置默认密码不参与（清单里显式写的密码仍保留，作为私钥之后的兜底） |
| 输入记忆（field memory） | 逐节点解析循环中跨节点累积的状态，记录「是否把本次交互输入应用到后续空字段节点」。端口 / 用户名 / 密码各持一组独立记忆 |
| 执行模式（execution mode） | 一次运行的四种操作之一：命令 / 脚本 / 上传 / 下载。四者互斥，每次只选一种 |
| 执行身份（execution identity） | 命令以何种身份在目标机执行：`direct`（登录用户身份）、`sudo`（root 身份） |
| 目标位置（target path） | `-p` 参数指向的位置。方向随执行模式反转：上传时是**服务器上的目录**，下载时是**本地的目录** |
| 软链接过滤（link filtering） | 上传与下载两侧对链接类条目（软链接 / Windows 目录联接 / `.lnk` 快捷方式）的统一处理：**过滤掉、不传输、不中断**，但要留下痕迹——上传时提示打到终端，下载时写进该节点的 `output` 字段。两侧都只有**过滤后一个可传文件都不剩**（可传目标为 0）才报错 |
| 工具模式（tool mode） | 与批量执行解耦、单独使用后即退出的两个操作：生成主密钥、转换凭据文件。二者不参与批量执行流程 |
| 认证失败分类（auth failure classification） | 私钥与密码**都尝试且都失败**时，由 SSH 层在结果中直接给出的短分类文案「密钥与密码均失败」。单一凭据失败（只私钥 / 只密码）或非认证类错误不带分类，仍按原文错误呈现 |
| 退出码（exit code） | 仅描述**命令执行结果**的权威信号：`0`=命令成功；非 `0`=命令失败；`nil`=未执行任何命令（连接失败 / 传输失败 / 超时 / 中断） |
| 预处理失败（precheck failure） | 上传 / 下载流程中，建连之后、逐文件传输之前的命令执行失败，以真实非 `0` 退出码表达 |
| 传输失败（transfer failure） | SFTP 读写失败。无命令可执行，故无退出码 |
| 危险检测（dangerous check） | 向目标机下发命令**之前**，对命令内容做静态分类，产出「命令段 + 命中规则 + 风险等级」，用于提醒操作者。定位是操作助手，**不是**拦截恶意命令的防御闸门 |
| 命令风险等级（risk level） | `forbidden` / `high` / `medium` / `low` 四档。`forbidden` 是最强一档提醒：打印警告后直接退出程序；其余三档只影响提醒颜色与措辞强度，均需用户确认。等级是提醒强度，不是安全边界 |
| 命令段（command segment） | 待判定的最小命令单位：以真实命令词开头，已剥除环境变量赋值与前导包装命令，并递归解析 shell / 远端内部的命令字符串。判定以命令段为单位——纯文本参数（如 `echo "..."`）不参与判定 |
| 历史记录（history） | 每次执行自动归档的独立目录，目录名由「时间 + 执行模式 + 备注」构成，内含日志、终端输出、汇总报告、结果明细与资源备份 |

---

## 已定决策

见 `docs/adr/`。
